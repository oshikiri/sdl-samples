name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master
      - deploy-workflow

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0

    - name: Build clock
      run: |
        pushd clock/
        make
        popd
